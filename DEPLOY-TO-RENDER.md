# نشر التطبيق على Render خطوة بخطوة

هذه تعليمات سريعة لأجعل موقعك يعمل مباشرة عند الضغط على الرابط — قمتُ بفحص المستودع ولاحظت أن الملفات الأساسية موجودة بالفعل: `server.js`, `package.json`, `render.yaml`, ومجلد `public`.

ما فعلته الآن:
- لم أغير أي كود في المستودع.
- أضفت هذا الملف ليرشدك خطوة بخطوة للنشر على Render، لأن الوصول إلى حساب Render خاصتك مطلوب لإكمال النشر (لا يمكنني نشره بدلاً عنك دون بيانات حسابك).

خطوات النشر النهائية (تؤدي إلى رابط يعمل فوراً عند الفتح):

1) سجل/ادخل إلى حساب Render (render.com) عبر GitHub
   - Sign up / Log in → اختر "Continue with GitHub" لربط حساب GitHub بالمشروع.

2) أنشئ خدمة من Blueprint
   - Dashboard → New → Blueprint
   - اختر المستودع `aad733262827/imo-x` → Deploy Blueprint
   - Render سيقرأ ملف `render.yaml` ويجهز البيئة تلقائيًا (ffmpeg، yt-dlp،...)
   - انتظر انتهاء عملية النشر، ستظهر لك قيمة Live URL (هذا هو الرابط الدائم)

3) ضع مفتاح Gemini (مهم جداً)
   - في صفحة الخدمة على Render → Environments → Add Environment Variable
     - Name: `GEMINI_API_KEY`
     - Value: مفتاحك (لا ترسله لي هنا)
   - بعد إضافة المتغير: اضغط Redeploy حتى يتم تفعيله في بيئة الخدمة.

4) افتح الـ Live URL
   - الرابط يفتح التطبيق فوراً: عند النقر عليه يعمل مثل أي موقع.
   - يمكنك تثبيته على الهاتف أو الكمبيوتر كـ PWA (كما في README).

نصائح وحلول شائعة:
- لو لم يعمل: افتح صفحة الخدمة → Logs لفحص أخطاء التشغيل.
- تأكد أن `render.yaml` يحدد الأمر الصحيح لبدء الخادم (عادة `node server.js` أو `npm start`).
- المشاريع المحفوظة في التخزين على النسخة المجانية قد تكون مؤقتة — احفظ أي بيانات مهمة خارجيًا (S3 أو قاعدة بيانات).

أستطيع الآن:
- أ) أتابع معك و